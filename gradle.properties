# iOS/macOS-only Kotlin/Native tasks (e.g. iosSimulatorArm64Test) cannot run on the Linux CI
# host; they build on macOS (Codemagic). Silence the per-configuration "target disabled" warning
# so the Linux build output stays clean — the targets are intentionally macOS-only here.
kotlin.native.ignoreDisabledTargets=true
