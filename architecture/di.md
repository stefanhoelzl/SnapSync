# DI wiring — binary × port matrix

Generated by `./gradlew architectureDiagrams` from a source scan of the composition
roots (derived scope: a top-level `class`/`object` under `app/` or `test/world/` whose
leading KDoc's first paragraph names it a composition root). A cell marks a root that
constructs the type. Do not edit — the `:tools:diagrams` freshness test fails on drift.

THE POINT of this table: a row where one root differs from its siblings without a
documented reason is the per-tier-drift instrument — the class of defect behind the
per-tier reconciler regression and the harness-mirror drift. The shared `compose/`
functions hold the feature graph; this matrix keeps the per-root adapter surface visible.

Roots found:

- `SnapSyncRoot` — `app/ios/src/iosMain/kotlin/app/snapsync/ios/SnapSyncRoot.kt`
- `UploadExtensionRoot` — `app/ios/extension/src/iosMain/kotlin/app/snapsync/ios/upload/UploadExtensionRoot.kt`

| Constructed type | SnapSyncRoot | UploadExtensionRoot |
|---|---|---|
| `AlbumCoordinator` | | x |
| `AlbumMapService` | x | x |
| `AppGraphReads` | x | |
| `AppPorts` | x | |
| `AttestState` | x | x |
| `CachedAttestStore` | | x |
| `ConfigService` | x | x |
| `DeviceManifestService` | x | x |
| `DownloadService` | x | |
| `EntryHooks` | x | |
| `FileLogSink` | x | x |
| `GalleryAlbums` | | x |
| `GalleryDiscovery` | | x |
| `HttpBackend` | x | x |
| `IosBackgroundTime` | x | |
| `IosDatabases` | x | x |
| `IosDeviceIntegrity` | x | |
| `IosDownloadTransport` | x | |
| `IosFiles` | x | x |
| `IosGallery` | x | |
| `IosGalleryReader` | x | x |
| `IosPreferences` | x | x |
| `IosProcessInfo` | x | |
| `IosPushRegistrationRecord` | x | |
| `IosSystemUi` | x | |
| `LedgerService` | x | x |
| `LogTailService` | x | |
| `MetricKitProcessMetrics` | x | |
| `NoPlatformDeviceId` | x | x |
| `OsDrivenRegistration` | x | |
| `PersistedDeviceIdentity` | x | x |
| `PhotoLibraryPermission` | x | |
| `ProcessPorts` | x | x |
| `PublicNSLogSink` | x | x |
| `PushPorts` | x | |
| `PushTokenSource` | x | |
| `RigSwitches` | x | |
| `SentryCrashReporter` | x | x |
| `StagingService` | x | |
| `SuppressionService` | | x |
| `UploadPorts` | | x |
| `UploadRecordPorts` | x | |
| `UrlSessionUploadController` | x | |
