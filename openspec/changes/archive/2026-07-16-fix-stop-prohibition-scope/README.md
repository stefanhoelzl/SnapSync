# fix-stop-prohibition-scope

stop() must not destroy dedup state (ledger rows, stored bytes). The cursor is not dedup state, and the PhotoKit tier must clear it to repair the OS's job wipe. Narrow the prohibition to what it defends.
