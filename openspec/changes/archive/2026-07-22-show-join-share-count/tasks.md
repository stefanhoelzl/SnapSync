## 1. Domain: shareable-count read-model (`:domain`)

- [x] 1.1 Add a `Contribution`-based count helper in `feature/status` (or a small shared function) that counts the policy-admitted set from cheap asset facts — `assetId`, `creationDate`, `mediaSubtypes`, `mediaType`, pixel dims, `hasAdjustments` — reusing the same origin exclusions as `OwnDeviceGalleryStatusSource.compute` (no policy fork). Returns `0` for `Contribution.None` with no enumeration. **Done:** `feature/status/ShareableCount.kt` (`shareableCountFromAssets` / `shareableCountFromSnapshot`); policy refactored to a shared `isOriginExcludedFacts` core + new `originExcludedAssetIds(List<RawAsset>)` in `model/SelectionPolicy.kt`.
- [x] 1.2 Define a `ports/` seam for the candidate count: a cheap, cutoff-bounded gallery read that yields the asset facts the count needs **without** an `assetResourcesForAsset` read, plus a way to supply the cached denylisted-album asset-id set. **Done:** `RawAssetSource.factsSince(since)`; album set is an injected `suspend (String) -> Set<String>` on `ShareableCountSource`.
- [x] 1.3 Add a `ShareableCount` query use-case: given a candidate cutoff + direction and the current `PermissionStatus`, produce `Int?` (`null` when the grant does not permit a count). Branch GRANTED → cheap fetch, LIMITED → filter the held selection snapshot, DENIED/unresolved → `null`. **Done:** `ShareableCountSource.count(...)`.
- [x] 1.4 Unit-test 1.1–1.3 in `commonTest` (JVM + iosSimulatorArm64) over `:adapter:generic:fake`: count equals the admitted set for several cutoffs; `0` for non-contributing; denylist subtracted; zero enumeration for `None`. **Done:** `domain/commonTest/.../ShareableCountTest.kt` (JVM green; existing `SelectionPolicyTest` still green after the refactor).

## 2. iOS adapter: count-only enumeration + LIMITED snapshot filtering

- [x] 2.1 In `:adapter:ios:ext-safe`, add a count/facts path to the PhotoKit enumerator that issues the cutoff-bounded `PHFetchOptions` fetch and reads only cheap `PHAsset` properties, skipping `assetResourcesForAsset`; resolve the denylisted-album set once and pass it in. **Done:** `PhotoLibraryRawAssetSource.factsSince` / `factAssetsFrom` (empty `rawResources`); compiles via `compileIosMainKotlinMetadata`.
- [x] 2.2 In `:adapter:ios:app-only`, expose filtering of the already-held selection snapshot by a candidate cutoff for the LIMITED path — no fresh `PHAsset` read (honour `limited-photo-access` rule ①). **Done:** `shareableCountFromSnapshot` filters the snapshot; `AppCore.loadShareableCount` feeds it `latestSelectionSnapshot.value` (the cell the LIMITED observer already populates) under a LIMITED grant — no new read.
- [x] 2.3 Ensure the compute runs off the main thread (sync-I/O port owns its dispatcher hop) so a far-back cutoff never touches the scene watchdog. **Done:** `factsSince` hops `Dispatchers.Default` like the other walks.

## 3. Presentation: the query + Ready state (`:ui:presentation`)

- [x] 3.1 Add a `loadShareableCount(candidateCutoff, direction)` suspend query to `StatusContainerHost`'s injected inputs (built only in `compose/`, same pattern as `loadJoinDetails`). **Done (refined):** the candidate cutoff is *screen-local* state, so the query is injected into `StatusScreen` (like `CutoffFormatter`), not the host — cleaner, no reducer change. `AppCore.loadShareableCount` is the query; forge/live routed via `SnapSyncRoot`'s `Shell`.
- [x] 3.2 Add a count field to `JoinPhase.Ready` (and the switch/reconfigure state): `Unavailable | Counting | Count(n)`. Recompute (debounced) on cutoff change and Share toggle; subscribe to the permission `StateFlow` so a late grant makes it appear. **Done (refined):** the count is *screen-local* `CountState` in `ShareCountRow`, recomputed via `LaunchedEffect(chosenCutoff, permissionKey)` (permission threaded as the trigger for a late grant) — no `UiState` field, keeping the health surface count-free.
- [x] 3.3 Wire the live `ShareableCount` query instance and its ports in `compose/SnapSyncApp.kt`. **Done:** `ShareableCountSource` + `loadShareableCount` + `photoPermission` on `AppCore`; new `AppPorts.rawFactsSince` bound in `SnapSyncRoot` (a `PhotoLibraryRawAssetSource`) and `World` (`gallery.source::factsSince`); `MainViewController` / `StatusPane` / `FullStackHarness` pass it through.

## 4. UI: the count row (`:ui:screens`, `:ui:components`)

- [x] 4.1 Add the count row under the Share switch in `ReadyLayout`: `XX photos from your gallery will be shared`; zero-state `0 …` + `New photos you take will be shared as you go`; `counting…`; hidden when Share off; omitted when unavailable. **Done:** `ShareCountRow` (`AppSectionNote` lines).
- [x] 4.2 Render the same row on the switch-event surface (`SwitchDialog`) and the reconfigure surface (`ReconfigureScreen`). **Done:** `ReconfigureScreen` uses `ShareCountRow`; `SwitchDialog` folds `shareCountSentence` into its body (the compact dialog has no room for the full row).
- [x] 4.3 Update/extend `JoinScreenTest` (jvmTest) for each state: positive count, zero-with-gloss, counting, hidden-when-off, omitted-when-unavailable. **Done:** 5 new cases (shows count, zero+gloss, unavailable→no row, hidden-when-share-off, recomputes-on-cutoff-change incl. singular); all green.

## 5. Fix: reconfigure cutoff-lowering backfill (tier-agnostic)

- [x] 5.1 Add a discovery-cursor invalidation seam reachable from `feature/membership` (a `ports/` method on the discovery store; built in `compose/`). **Done:** reused `DiscoveryStore.clearToken()`; exposed as `AppPorts.clearDiscoveryCursor` (bound in `SnapSyncRoot` via `IosDiscoveryStore`, in `World` via `discoveryStore::clearToken`).
- [x] 5.2 In `ReconfigureEvent`, when the new `minPhotoDate` is earlier than the current, invalidate the discovery cursor so the next cycle full-re-enumerates at the new cutoff. No invalidation on raising the cutoff. **Done:** lexicographic `newCfg.minPhotoDate < current.minPhotoDate` gate, cleared before `armUpload`.
- [x] 5.3 `commonTest` / `:test:integration`: lowering the cutoff on reconfigure re-enumerates and uploads the newly-in-scope older photos on **both** tiers, while `COMPLETED` rows suppress re-upload of already-shared ones; raising the cutoff forces no re-enumeration and un-shares nothing. **Done (unit):** `ReconfigureEventTest` — lower→clearCursor-before-arm, raise→no-clear, unchanged→no-clear. *(End-to-end re-upload-through-the-world is the group-6 integration test.)*

## 6. Integration + policy identity (`:test:integration`)

- [x] 6.1 Integration test: the count shown for a candidate cutoff equals the actual upload set produced by the cycle for that same cutoff (one-universe identity). **Done:** `ShareableCountIntegrationTest` (count==cycle-upload-set; later cutoff previews fewer).
- [x] 6.2 Integration test: DENIED / unresolved permission yields no count and triggers no library read. **Done:** `no_count_without_a_usable_grant`. *(Note: the world's in-memory `RawAssetSource` always full-walks, so it can't reproduce the tier-specific forward-only-cursor bug — the backfill fix is covered at the unit level in `ReconfigureEventTest`, which is the faithful level for it.)*

## 7. Specs, gates, ship

- [x] 7.1 Author the `join-share-count` spec Purpose (replace any minted placeholder) at sync/archive; run validate. **Done at archive:** placeholder replaced with a real Purpose; `validate --specs --strict` = 59/59.
- [x] 7.2 `./gradlew build` (all targets + JVM tests + architecture guards) and `./gradlew compileIosMainKotlinMetadata` green; run `./gradlew architectureDiagrams` and commit if changed. **Done:** full `./gradlew build` green (`BUILD_EXIT=0`); iOS metadata compiles; `architectureDiagrams` regenerated (`architecture/di.md`, `features.md` updated — staged for the ship commit).
- [x] 7.3 Account for touched modules against capabilities at archive time (delta-completeness gate). **Done:** all touched modules accounted (photo-selection-policy/join-share-count/reconfigure-membership/join-event deltas; gallery-status behavior-preserving; shells/harness/fakes wiring). `ios-url-session-upload` needs no delta — the cursor invalidation is triggered externally by `ReconfigureEvent` (reconfigure-membership), not by the tier's own producer. Dead-types gate clean (no removed declarations); placeholder gate clean.
- [x] 7.4 On-device sanity (SE2, iOS 26.5.2, PhotoKit tier). **Done (non-destructive):** built this branch via ssh-mac (Xcode 26.5 archive **SUCCEEDED** — a stronger check than the Linux proxy), re-signed inside-out (no wildcard leak), installed + launched — my build runs, mints an event, no crash. Then minted an event with `startsAt`=2020 and opened its link while joined, raising the **switch dialog with the live count over the real library**: *"27 photos from your gallery will be shared"* (real PhotoKit walk via the cheap `factsSince`, policy applied, count rendered). Did **not** confirm the switch, so the real membership stayed intact ("In sync"); relaunched to clear the transient pending-switch. The interactive Ready-surface count row and the reconfigure-backfill upload are **tap-gated** (need a signed WebDriverAgent, not set up) and/or would require leaving the real membership, so they were verified via the switch-dialog count sentence + the unit/integration tests instead.
- [ ] 7.5 Branch → PR → `/ship`.
