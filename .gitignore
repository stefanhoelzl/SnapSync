.gradle/
build/
gradle/wrapper/gradle-wrapper.jar
local.properties
*.hprof
.idea/
.kotlin/

# backend bundle output (built in CI, deployed; never committed)
backend/dist/

# Derived from the committed raws in screenshots/ by `deno task shots` (capability marketing-site).
