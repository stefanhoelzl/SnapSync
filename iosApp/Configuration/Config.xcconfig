// Shared build configuration consumed by iosApp.xcodeproj.
APP_NAME = SnapSync
BUNDLE_ID = app.snapsync
// Apple Team ID — drives DEVELOPMENT_TEAM for signed device/TestFlight builds.
// Not a secret (visible in any shipped IPA); the simulator gate ignores it (CODE_SIGNING_ALLOWED=NO).
TEAM_ID = E9Z8BADH58
// Keychain-sharing + App-Group entitlement (deeplink-config). Path is relative to SRCROOT (iosApp/).
// The App ID must have Keychain Sharing + App Groups (group.app.snapsync) capabilities enabled for
// signed device/TestFlight builds; the simulator gate ignores signing.
CODE_SIGN_ENTITLEMENTS = iosApp/iosApp.entitlements
